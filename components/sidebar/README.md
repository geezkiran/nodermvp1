# Sidebar Module

This folder keeps sidebar concerns split for easier maintenance and scaling.

- `types.ts`: shared contracts
- `config.ts`: menu and section content
- `icons.tsx`: icon mapping
- `index.tsx`: composed UI
